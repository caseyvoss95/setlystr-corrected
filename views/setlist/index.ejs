<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/setlist/setlist.css">
    <link rel="stylesheet" href="/setlist/index.css">
    <title>index</title>
</head>

<body>
    <h1>setlistyr</h1>
    <div class="row">
        <div class="column">
            <h2>desired setlist</h2>
            <table>
                <tbody>
                    <% for (i=0; i < songs.length; i++) {%>
                        <% if (songs[i].inSetlist) {%>
                            <tr>
                                <td>
                                    <a href="/setlist/<%=songs[i]._id%>">
                                        <%=songs[i].title%>
                                    </a>
                                    <a href="/setlist/<%=songs[i]._id%>/rem"><button>delete</button></a>
                                </td>
                            </tr>
                            <%}%>
                        <%}%>
                </tbody>
            </table>
        </div>
        <div class="column">
            <h2>available songs</h2>
            <table>
                <tbody>
                    <% for (i=0; i < songs.length; i++) {%>
                        <tr>
                            <td>
                                <a href="/setlist/<%=songs[i]._id%>">
                                    <%=songs[i].title%>
                                </a>
                                <form action="/setlist/<%=songs[i]._id%>?_method=DELETE" method="POST">
                                    <input type="submit" value="delete">
                                </form>
                                <a href="/setlist/<%=songs[i]._id%>/add"><button>add</button></a>
                            </td>
                        </tr>
                        <%}%>
                </tbody>
            </table>
        </div>

    </div>
    <p>Total Duration: <%=totalDuration%> minutes</p>
    <p>Total Cost: $<%=Math.round(100*cost) / 100%>
    </p>
    <a href="/setlist/new"><button id="create">add new song</button></a>

</body>

</html>